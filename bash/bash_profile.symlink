# Add symlink files to path
for file in ~/.*.path; do
    [ -f "$file" ] || break
    source $file
done
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

alias vsh="docker run --rm -ti -v ~/.vsh/workspace/:/workspace vivint/vsh vsh"
export PATH="$HOME/.cargo/bin:$PATH"
